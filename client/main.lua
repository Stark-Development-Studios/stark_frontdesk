local Config = require 'shared.config'

if Config.Framework == 'qb' then
    -- Functions
    -- Events
    -- Threads
end

if Config.Framework == 'qbx' then
    -- Functions
    -- Events
    -- Threads
end
